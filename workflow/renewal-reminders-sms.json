{
  "name": "Template: License and insurance renewal reminders by SMS",
  "nodes": [
    {
      "id": "d9f530a5-33b7-54e8-b375-dccd4f91a08e",
      "name": "Every morning at 8",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        200,
        300
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "days",
              "triggerAtHour": 8
            }
          ]
        }
      }
    },
    {
      "id": "5fb43385-1b01-5b02-981e-4af8e67b2dec",
      "name": "Done link tapped",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        200,
        520
      ],
      "webhookId": "15901f1f-41e1-5091-81df-76595da9e282",
      "parameters": {
        "path": "renewal-done",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "6f78db43-aba0-5c69-aa50-df7906893eee",
      "name": "Your settings",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        420,
        400
      ],
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "3fe82a85-d501-506a-b131-e13e87e71660",
              "name": "business_name",
              "type": "string",
              "value": "Your Business"
            },
            {
              "id": "75860232-7ab3-56c6-83ce-1da4e24d794a",
              "name": "business_number",
              "type": "string",
              "value": "+15555550100"
            },
            {
              "id": "f0baa9aa-45f6-594f-aa85-8ce516bff470",
              "name": "owner_cell",
              "type": "string",
              "value": "+15555550199"
            },
            {
              "id": "34edc3ae-f35f-5e61-8bcb-eeba62e15e87",
              "name": "timezone",
              "type": "string",
              "value": "America/New_York"
            },
            {
              "id": "c67fe09c-6a28-5e15-b5b8-a4566eef0929",
              "name": "n8n_url",
              "type": "string",
              "value": "https://your-instance.app.n8n.cloud"
            },
            {
              "id": "0ed81844-1325-5b7d-af20-bab3648a0c32",
              "name": "items",
              "type": "string",
              "value": "Contractor license | 2027-06-30 | every 2 years | Renew at the state licensing board\nGeneral liability insurance | 2027-01-15 | yearly | Call your agent\nCity business license | 2027-01-01 | yearly\nTruck registration | 3/31/2027 | yearly"
            },
            {
              "id": "688ade80-bfd0-56fb-b7df-685bbc9e0afd",
              "name": "remind_days",
              "type": "string",
              "value": "60, 30, 14, 7, 3, 1, 0"
            },
            {
              "id": "1681dc8f-5ee4-50e1-bfb5-e109d3dfa53e",
              "name": "overdue_every_days",
              "type": "number",
              "value": 1
            },
            {
              "id": "87c6735d-109d-52a6-882e-28fc0bf1ce6f",
              "name": "monthly_outlook",
              "type": "boolean",
              "value": true
            },
            {
              "id": "68095f7b-b5dc-5046-814b-7b3dd8261be3",
              "name": "outlook_days",
              "type": "number",
              "value": 90
            },
            {
              "id": "993c7346-d483-569a-8cd0-e8be01445c15",
              "name": "done_links",
              "type": "boolean",
              "value": true
            }
          ]
        },
        "options": {}
      }
    },
    {
      "id": "d03621df-7138-53f6-b359-95bfd29bd98d",
      "name": "Read your list",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        400
      ],
      "parameters": {
        "jsCode": "// Reads Your settings and your list of renewals. Checks the settings, turns each line of the list\n// into a dated item, and works out whether this run came from the daily schedule or a tapped done link.\nconst s = $('Your settings').first().json;\nconst ran = name => {\n  try {\n    const n = $(name);\n    return typeof n.isExecuted === 'boolean' ? n.isExecuted : (n.first(), true);\n  } catch (e) { return false; }\n};\n\nconst phone = v => {\n  const raw = String(v || '').trim();\n  const digits = raw.replace(/\\D/g, '');\n  if (raw.startsWith('+') && digits.length >= 8 && digits.length <= 15) return '+' + digits;\n  if (digits.length === 10) return '+1' + digits;\n  if (digits.length === 11 && digits.startsWith('1')) return '+' + digits;\n  return '';\n};\n// A blank number setting falls back to the default, but 0 is kept, because 0 switches something off.\nconst num = (v, d) => (v === '' || v === null || v === undefined || isNaN(Number(v))) ? d : Number(v);\nconst yes = v => v !== false && v !== 'false';\n\nconst business_number = phone(s.business_number);\nconst owner_cell = phone(s.owner_cell);\nif (!business_number || !owner_cell) {\n  throw new Error('Your settings: business_number and owner_cell must both be phone numbers, like +15551234567.');\n}\nif (['+15555550100', '+15555550199'].includes(business_number) || ['+15555550100', '+15555550199'].includes(owner_cell)) {\n  throw new Error('Your settings: replace the example phone numbers with your Twilio number and your cell before you publish this.');\n}\nconst timezone = String(s.timezone || 'America/New_York').trim();\nif (!DateTime.now().setZone(timezone).isValid) {\n  throw new Error('Your settings: \"' + timezone + '\" is not a time zone n8n knows. Use a name like America/New_York.');\n}\nconst done_links = yes(s.done_links);\nconst n8n_url = String(s.n8n_url || '').trim().replace(/\\/+$/, '');\nif (done_links && (!/^https?:\\/\\/[a-z0-9.-]+(:\\d+)?$/i.test(n8n_url) || /your-instance/i.test(n8n_url))) {\n  throw new Error('Your settings: set n8n_url to the address of your n8n, like https://yourname.app.n8n.cloud, or set done_links to false.');\n}\nconst remind_days = [...new Set(String(s.remind_days || '60, 30, 14, 7, 3, 1, 0')\n  .split(/[\\s,]+/).filter(Boolean).map(Number).filter(n => Number.isInteger(n) && n >= 0))].sort((a, b) => b - a);\n\nconst cfg = {\n  business_name: String(s.business_name || 'your business').trim(),\n  business_number, owner_cell, timezone, n8n_url, done_links,\n  remind_days: remind_days.length ? remind_days : [0],\n  overdue_every_days: Math.max(0, num(s.overdue_every_days, 1)),\n  monthly_outlook: yes(s.monthly_outlook),\n  outlook_days: Math.max(1, num(s.outlook_days, 90)),\n};\n\n// Dates: 2027-06-30 or 6/30/2027. Repeats: yearly, monthly, quarterly, every 2 years, every 6 months, none.\nconst readDate = t => {\n  t = String(t || '').trim();\n  let d = null;\n  if (/^\\d{4}-\\d{1,2}-\\d{1,2}$/.test(t)) { const [y, m, dd] = t.split('-').map(Number); d = DateTime.fromObject({ year: y, month: m, day: dd }, { zone: timezone }); }\n  else if (/^\\d{1,2}\\/\\d{1,2}\\/\\d{4}$/.test(t)) { const [m, dd, y] = t.split('/').map(Number); d = DateTime.fromObject({ year: y, month: m, day: dd }, { zone: timezone }); }\n  return d && d.isValid ? d.toISODate() : null;\n};\nconst UNITS = { day: 'days', days: 'days', week: 'weeks', weeks: 'weeks', month: 'months', months: 'months', year: 'years', years: 'years' };\nconst readRepeat = t => {\n  t = String(t || '').trim().toLowerCase();\n  if (!t || ['none', 'once', 'no', 'never', 'one time', '-'].includes(t)) return { ok: true, repeat: null };\n  if (['yearly', 'annually', 'annual', 'every year', 'each year'].includes(t)) return { ok: true, repeat: { years: 1 } };\n  if (['monthly', 'every month', 'each month'].includes(t)) return { ok: true, repeat: { months: 1 } };\n  if (['quarterly', 'every quarter'].includes(t)) return { ok: true, repeat: { months: 3 } };\n  const m = t.match(/^(?:every\\s+)?(\\d{1,3})\\s+(days?|weeks?|months?|years?)$/);\n  if (m && Number(m[1]) > 0) return { ok: true, repeat: { [UNITS[m[2]]]: Number(m[1]) } };\n  return { ok: false, repeat: null };\n};\nconst slug = t => String(t).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 40) || 'item';\n\nconst items = [], problems = [], seen = {};\nString(s.items || '').split(/\\r?\\n/).forEach((line, i) => {\n  const text = line.trim();\n  if (!text || text.startsWith('#')) return;\n  const [name = '', date = '', repeat = '', ...rest] = text.split('|').map(x => x.trim());\n  const note = rest.join(' | ').trim();\n  const due = readDate(date);\n  const r = readRepeat(repeat);\n  if (!name) { problems.push('Line ' + (i + 1) + ' has no name.'); return; }\n  if (!due) { problems.push('\"' + name + '\": I could not read the date \"' + date + '\". Use 2027-06-30 or 6/30/2027.'); return; }\n  if (!r.ok) problems.push('\"' + name + '\": I could not read \"' + repeat + '\" as a repeat, so it will not roll forward. Use yearly, monthly, quarterly, every 2 years or none.');\n  let key = slug(name);\n  seen[key] = (seen[key] || 0) + 1;\n  if (seen[key] > 1) key += '-' + seen[key];\n  items.push({ key, name, due, repeat: r.repeat, note });\n});\n\nconst tapped = ran('Done link tapped');\nconst query = tapped ? ($('Done link tapped').first().json.query || {}) : {};\nreturn [{ json: { mode: tapped ? 'tap' : 'daily', cfg, items, problems, query } }];\n"
      }
    },
    {
      "id": "b1b41018-b16b-5bd3-b7c8-dc29129a8fbe",
      "name": "Tapped a done link?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        860,
        400
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "7766ea39-b9c3-52f9-b789-7d4791f46f66",
              "leftValue": "={{ $json.mode === 'tap' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "c1c3862b-ad4a-56ec-969b-27937a9d5f5a",
      "name": "Decide what to remind you",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        300
      ],
      "parameters": {
        "jsCode": "// Runs once a day. Works out which renewals are on a reminder day, overdue, or coming up, and\n// writes one text. Its memory (rolled-forward dates, done links, what already went out) is n8n\n// workflow static data, which n8n keeps only while the workflow is published.\nconst { cfg, items, problems } = $('Read your list').first().json;\nconst st = $getWorkflowStaticData('global');\nst.items = st.items || {};\nconst tz = cfg.timezone;\nconst today = DateTime.now().setZone(tz).startOf('day');\nconst todayISO = today.toISODate();\nconst day = iso => DateTime.fromISO(iso, { zone: tz }).startOf('day');\nconst nice = iso => day(iso).toFormat(day(iso).year === today.year ? 'ccc LLL d' : 'ccc LLL d, yyyy');\nconst plural = (n, w) => n + ' ' + w + (n === 1 ? '' : 's');\nconst makeNonce = () => {\n  const c = globalThis.crypto;\n  if (c && c.getRandomValues) return Array.from(c.getRandomValues(new Uint8Array(12)), b => (b % 36).toString(36)).join('');\n  return Array.from({ length: 12 }, () => Math.floor(Math.random() * 36).toString(36)).join('');\n};\n\n// A run that already happened today does nothing, so a second trigger never doubles the text.\nif (st.last_run_day === todayISO) {\n  return [{ json: { send: false, body: '', reason: 'already ran today', from: cfg.business_number, to: cfg.owner_cell } }];\n}\nst.last_run_day = todayISO;\nconst first = !st.started;\n\n// Work out each item's current due date and how far away it is.\nconst live = [];\nfor (const it of items) {\n  const mem = st.items[it.key] || {};\n  let due = it.due;\n  // A done link rolled this item forward. The rolled date stands until someone changes the\n  // date in the list, and then the list wins, in either direction.\n  if (mem.due && mem.list_due === it.due) due = mem.due;\n  else if (mem.due) { delete mem.due; delete mem.list_due; }\n  if (mem.done_for && mem.done_for !== due) delete mem.done_for;\n  st.items[it.key] = mem;\n  const days = Math.round(day(due).diff(today, 'days').days);\n  live.push({ ...it, due, days, done: mem.done_for === due, mem });\n}\n// Forget items that are no longer on the list.\nfor (const k of Object.keys(st.items)) if (!items.some(it => it.key === k)) delete st.items[k];\n\nconst linkFor = x => {\n  if (!cfg.done_links) return '';\n  if (!x.mem.nonce || x.mem.nonce_due !== x.due) { x.mem.nonce = makeNonce(); x.mem.nonce_due = x.due; }\n  return ' Done? ' + cfg.n8n_url + '/webhook/renewal-done?i=' + encodeURIComponent(x.key) + '&n=' + x.mem.nonce;\n};\nconst when = x => x.days > 1 ? 'due in ' + x.days + ' days (' + nice(x.due) + ')'\n  : x.days === 1 ? 'due tomorrow (' + nice(x.due) + ')'\n  : x.days === 0 ? 'due today'\n  : plural(-x.days, 'day') + ' overdue (was due ' + nice(x.due) + ')';\n\nconst due = live.filter(x => !x.done && (\n  cfg.remind_days.includes(x.days) ||\n  (x.days < 0 && cfg.overdue_every_days > 0 && (-x.days) % cfg.overdue_every_days === 0)));\nconst paragraphs = [];\n\nif (first) {\n  const next = live.filter(x => !x.done).sort((a, b) => a.days - b.days).slice(0, 3)\n    .map(x => x.name + ' (' + (x.days < 0 ? 'overdue since ' + nice(x.due) : nice(x.due) + ', in ' + plural(x.days, 'day')) + ')');\n  paragraphs.push('Renewal reminders are on for ' + cfg.business_name + '. Tracking ' + plural(live.length, 'item') + '.' +\n    (next.length ? ' Next up: ' + next.join(', ') + '.' : '') +\n    ' You get a text ' + cfg.remind_days.join(', ') + ' days before each one' +\n    (cfg.monthly_outlook ? ', and on the 1st of each month a look at the next ' + cfg.outlook_days + ' days.' : '.'));\n  st.started = todayISO;\n}\n\nif (due.length) {\n  const lines = due.sort((a, b) => a.days - b.days).map(x => '- ' + x.name + ': ' + when(x) + '.' + (x.note ? ' ' + x.note.replace(/[.!?\\s]+$/, '') + '.' : '') + linkFor(x));\n  paragraphs.push('Renewals:\\n' + lines.join('\\n'));\n}\n\nif (cfg.monthly_outlook && (today.day === 1 || first)) {\n  const soon = live.filter(x => !x.done && x.days >= 0 && x.days <= cfg.outlook_days && !due.includes(x))\n    .sort((a, b) => a.days - b.days).map(x => x.name + ' ' + nice(x.due));\n  if (soon.length && !first) paragraphs.push('Coming up in the next ' + cfg.outlook_days + ' days: ' + soon.join(', ') + '.');\n  if (!soon.length && !due.length && !first) paragraphs.push('Nothing on your renewal list is due in the next ' + cfg.outlook_days + ' days.');\n}\n\n// Lines in the list that could not be read: said once, and again whenever they change.\nconst sig = problems.join('|');\nif (problems.length && sig !== st.problem_sig) paragraphs.push('Check your renewal list:\\n- ' + problems.join('\\n- '));\nst.problem_sig = sig;\n\n// Anything Twilio would not take last time goes again, for up to 3 days.\nst.unsent = (st.unsent || []).filter(u => day(u.day).diff(today, 'days').days > -3);\nconst all = [...st.unsent.map(u => u.text), ...paragraphs];\nst.unsent = [...st.unsent, ...paragraphs.map(text => ({ text, day: todayISO }))];\nlet body = all.join('\\n\\n');\nif (body.length > 1500) body = body.slice(0, 1480) + '... (cut short)';\n\nreturn [{ json: {\n  send: body.length > 0,\n  body,\n  from: cfg.business_number,\n  to: cfg.owner_cell,\n  first_run: first,\n  reminded: due.map(x => x.key),\n  tracked: live.length,\n} }];\n"
      }
    },
    {
      "id": "c22ca99a-6ac6-5e74-8368-716cc10d1766",
      "name": "Anything to send?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1320,
        300
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "4e38a8a6-fbb7-570c-9b0a-101312718e13",
              "leftValue": "={{ $json.send }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "62b4baf5-2a13-5d30-add6-4fa0a4ca6782",
      "name": "Find your Twilio account",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1540,
        300
      ],
      "parameters": {
        "method": "GET",
        "url": "https://api.twilio.com/2010-04-01/Accounts.json",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "twilioApi",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "Status",
              "value": "active"
            },
            {
              "name": "PageSize",
              "value": "1"
            }
          ]
        },
        "options": {}
      }
    },
    {
      "id": "68fb9185-5350-58bf-84a3-7b74e6e809f2",
      "name": "Text you",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1760,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "=https://api.twilio.com/2010-04-01/Accounts/{{ $('Find your Twilio account').first().json.accounts[0].sid }}/Messages.json",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "twilioApi",
        "sendBody": true,
        "contentType": "form-urlencoded",
        "bodyParameters": {
          "parameters": [
            {
              "name": "From",
              "value": "={{ $('Decide what to remind you').first().json.from }}"
            },
            {
              "name": "To",
              "value": "={{ $('Decide what to remind you').first().json.to }}"
            },
            {
              "name": "Body",
              "value": "={{ $('Decide what to remind you').first().json.body }}"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      }
    },
    {
      "id": "f5d7ffe6-b5c7-5e15-aec0-5da8a21a4d44",
      "name": "Went out?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1980,
        300
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "39e63513-8e9b-5125-825b-2d6e8e6d1a38",
              "leftValue": "={{ !!$json.sid }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "ebca5b04-6ab3-51b7-9b22-c3a0348b8574",
      "name": "Mark it delivered",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2200,
        300
      ],
      "parameters": {
        "jsCode": "// The text went out, so nothing is waiting to be sent again.\n$getWorkflowStaticData('global').unsent = [];\nreturn [{ json: { delivered: true, sid: $json.sid } }];\n"
      }
    },
    {
      "id": "5f4a9c2a-a696-5dfb-9749-8e6c664b3240",
      "name": "Send it again tomorrow",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        2200,
        500
      ],
      "parameters": {}
    },
    {
      "id": "8b338fa6-cd36-5a6b-89fa-bdba62a0ea56",
      "name": "Mark it done",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        860
      ],
      "parameters": {
        "jsCode": "// Runs when the owner taps a Done link, or the Undo link on the page that follows. A Done link\n// must be the current one for that item: a repeating item rolls forward by its repeat, a one-time\n// item is marked finished. The page says what changed and offers an Undo for a mistaken tap.\nconst { cfg, items, query } = $('Read your list').first().json;\nconst st = $getWorkflowStaticData('global');\nst.items = st.items || {};\nconst tz = cfg.timezone;\nconst today = DateTime.now().setZone(tz).startOf('day');\nconst nice = iso => DateTime.fromISO(iso, { zone: tz }).toFormat('cccc, LLLL d, yyyy');\nconst esc = t => String(t).replace(/[&<>\"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' }[c]));\nconst makeNonce = () => {\n  const c = globalThis.crypto;\n  if (c && c.getRandomValues) return Array.from(c.getRandomValues(new Uint8Array(12)), b => (b % 36).toString(36)).join('');\n  return Array.from({ length: 12 }, () => Math.floor(Math.random() * 36).toString(36)).join('');\n};\nconst page = (title, text, undoUrl) => '<!doctype html><html><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">' +\n  '<title>' + esc(title) + '</title></head><body style=\"font-family:system-ui,sans-serif;max-width:32rem;margin:3rem auto;padding:0 1.25rem;line-height:1.5;color:#1a1a1a\">' +\n  '<h1 style=\"font-size:1.4rem\">' + esc(title) + '</h1><p style=\"font-size:1.1rem\">' + esc(text) + '</p>' +\n  (undoUrl ? '<p style=\"margin-top:2rem\"><a href=\"' + esc(undoUrl) + '\" style=\"color:#555\">Tapped this by mistake? Undo</a></p>' : '') +\n  '</body></html>';\nconst nothing = () => [{ json: { ok: false, html: page('Nothing changed', 'This link was already used, or the date has changed since it was sent. If something still needs updating, change its date in your renewal list.') } }];\n\nconst key = String(query.i || '');\nconst item = items.find(x => x.key === key);\nconst mem = st.items[key];\nif (!item || !mem) return nothing();\n\n// Undo: put the item back exactly as it was before the Done tap.\nif (query.u) {\n  if (!mem.undo || mem.undo.nonce !== String(query.u)) return nothing();\n  const prev = mem.undo.prev;\n  for (const k of ['due', 'list_due', 'done_for', 'nonce', 'nonce_due']) { if (prev[k] === undefined) delete mem[k]; else mem[k] = prev[k]; }\n  delete mem.undo;\n  const due = mem.due || item.due;\n  return [{ json: { ok: true, key, undone: true, html: page('Undone: ' + item.name, 'It is back to due ' + nice(due) + ', and the reminders carry on as before.') } }];\n}\n\nconst nonce = String(query.n || '');\nif (!mem.nonce || !nonce || mem.nonce !== nonce) return nothing();\nconst prev = {};\nfor (const k of ['due', 'list_due', 'done_for', 'nonce', 'nonce_due']) if (mem[k] !== undefined) prev[k] = mem[k];\nconst was = mem.nonce_due;\ndelete mem.nonce;\ndelete mem.nonce_due;\nmem.undo = { nonce: makeNonce(), prev };\nconst undoUrl = cfg.n8n_url + '/webhook/renewal-done?i=' + encodeURIComponent(key) + '&u=' + mem.undo.nonce;\n\nif (item.repeat) {\n  let next = DateTime.fromISO(was, { zone: tz });\n  do { next = next.plus(item.repeat); } while (next <= today);\n  mem.due = next.toISODate();\n  mem.list_due = item.due;\n  delete mem.done_for;\n  // The first reminder for the new date is the earliest reminder day that is still ahead.\n  const ahead = cfg.remind_days.map(d => next.minus({ days: d })).filter(d => d > today);\n  const firstReminder = ahead.length ? ahead.reduce((a, b) => (b < a ? b : a)) : next;\n  return [{ json: { ok: true, key, next: mem.due, first_reminder: firstReminder.toISODate(), html: page('Done: ' + item.name,\n    'Next due ' + nice(mem.due) + '. The first reminder comes on ' + nice(firstReminder.toISODate()) + '. If that is not right, change the date in your renewal list and the list wins.', undoUrl) } }];\n}\nmem.done_for = was;\nreturn [{ json: { ok: true, key, next: null, html: page('Done: ' + item.name,\n  'No more reminders for it. It has no repeat, so when you get a chance, give it a new date or take it off your renewal list.', undoUrl) } }];\n"
      }
    },
    {
      "id": "58b18cd1-fed8-587d-a652-b4b86593652b",
      "name": "Show what changed",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        1320,
        860
      ],
      "parameters": {
        "respondWith": "text",
        "responseBody": "={{ $json.html }}",
        "options": {
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "text/html; charset=utf-8"
              }
            ]
          }
        }
      }
    },
    {
      "id": "9b87d33f-35d0-5bfe-a06c-26b689198e7d",
      "name": "Sticky Note",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        140,
        -330
      ],
      "parameters": {
        "content": "## Renewal reminders by text: licenses, insurance, registrations\n\n**Who it is for:** a service business that has to keep a handful of things current to keep working: the state contractor license, liability and workers' comp insurance, the city or county business license, truck tags, the company's annual filing, bonds, the domain name. Miss one and you can end up unable to pull a permit, or working a job without coverage.\n\n**How it works**\n1. Your list lives in **Your settings**, one line each: `name | due date | repeat | note`. Dates like 2027-06-30 or 6/30/2027. Repeat is yearly, monthly, quarterly, every 2 years, every 6 months, or none.\n2. Every morning at 8 it checks the list and texts you anything 60, 30, 14, 7, 3 or 1 days out, due today, or overdue. Overdue items come every day until they are handled.\n3. Each line of the text has a **Done?** link. Tap it once you have renewed. A repeating item rolls forward to its next due date and a one-time item stops, so the list does not need editing every year.\n4. On the 1st of each month you get a look at the next 90 days.\n5. The first morning it texts you what it is tracking, and it tells you about any line it cannot read.\n\nNo database. Rolled dates and links live in the workflow's own static data, which n8n keeps while the workflow is published. If you change an item's date in the list, the list wins.\n\n**Setup**\n1. Select your Twilio credential on the two HTTP Request nodes that talk to Twilio.\n2. Fill in **Your settings**: your Twilio number, your cell, your time zone, the address of your n8n (for the done links) and your list.\n3. Publish it. The first text arrives the next morning at 8.",
        "width": 1160,
        "height": 440,
        "color": 1
      }
    },
    {
      "id": "b27d80d4-c35e-5c04-a86f-1287285db755",
      "name": "Sticky Note1",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        140,
        200
      ],
      "parameters": {
        "content": "### 1. Your list\nThe morning schedule and a tapped done link both start here. Your settings are checked, and each line of the list becomes a dated item. A line it cannot read is reported to you, not skipped quietly.",
        "width": 880,
        "height": 440,
        "color": 7
      }
    },
    {
      "id": "de7bdfa2-55fb-5cc9-a41a-996ae1639c48",
      "name": "Sticky Note2",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1040,
        140
      ],
      "parameters": {
        "content": "### 2. The morning text\nOne text a day at most, even if the schedule fires twice. Items on a reminder day, overdue items, and on the 1st a look ahead. If Twilio will not take the text, it goes again the next morning, for up to 3 days.",
        "width": 1300,
        "height": 480,
        "color": 7
      }
    },
    {
      "id": "7be9e3ed-bd34-5665-bb31-3649f087eb60",
      "name": "Sticky Note3",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1040,
        700
      ],
      "parameters": {
        "content": "### 3. Done links\nEach link carries a one-time code for that item and that due date. A repeating item moves to its next date; a one-time item stops. The page says what changed and has an Undo link for a mistaken tap. A used or out-of-date link changes nothing.",
        "width": 520,
        "height": 340,
        "color": 7
      }
    },
    {
      "id": "3c33d136-b75b-534b-bed7-f0c014f1eae3",
      "name": "Sticky Note4",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        140,
        700
      ],
      "parameters": {
        "content": "### Trying it out\nAdd a test line due in 3 days, set the schedule to a few minutes from now and publish. One text arrives with a Done link. Tap it and read the page. Then put the schedule back to 8 AM.\n\nChanged a setting while it is published? Publish again, or the running version keeps the old list.",
        "width": 760,
        "height": 170,
        "color": 4
      }
    }
  ],
  "connections": {
    "Every morning at 8": {
      "main": [
        [
          {
            "node": "Your settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Done link tapped": {
      "main": [
        [
          {
            "node": "Your settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Your settings": {
      "main": [
        [
          {
            "node": "Read your list",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read your list": {
      "main": [
        [
          {
            "node": "Tapped a done link?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Tapped a done link?": {
      "main": [
        [
          {
            "node": "Mark it done",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Decide what to remind you",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark it done": {
      "main": [
        [
          {
            "node": "Show what changed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Decide what to remind you": {
      "main": [
        [
          {
            "node": "Anything to send?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Anything to send?": {
      "main": [
        [
          {
            "node": "Find your Twilio account",
            "type": "main",
            "index": 0
          }
        ],
        []
      ]
    },
    "Find your Twilio account": {
      "main": [
        [
          {
            "node": "Text you",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Text you": {
      "main": [
        [
          {
            "node": "Went out?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Went out?": {
      "main": [
        [
          {
            "node": "Mark it delivered",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Send it again tomorrow",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "templateCredsSetupCompleted": false
  }
}
